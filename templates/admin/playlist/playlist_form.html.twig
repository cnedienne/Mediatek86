{# empty Twig template #}
{{ form_start(playlistForm) }}

        <div class="mb-3">
            <label class="form-label fw-bold">{{ form_label(playlistForm.name) }}</label>
            {{ form_widget(playlistForm.name, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="mb-3">
            <label class="form-label fw-bold">{{ form_label(playlistForm.description) }}</label>
            {{ form_widget(playlistForm.description, {'attr': {'class': 'form-control', 'rows': '4'}}) }}
        </div>

       
        <div class="d-flex justify-content-between">
            <a href="{{ path('admin.playlists') }}" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Retour
            </a>
            <button type="submit" class="btn btn-success">
                <i class="bi bi-check-circle"></i> Enregistrer
            </button>
        </div>

        {{ form_end(playlistForm) }}